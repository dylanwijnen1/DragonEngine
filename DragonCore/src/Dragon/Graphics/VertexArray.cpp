#include "VertexArray.h"

#include <Dragon/Graphics/RenderTarget.h>

void dragon::VertexArray::Render(RenderTarget& target, RenderStates states)
{
	//target.Draw(m_vertices.data(), m_type, states);
}
